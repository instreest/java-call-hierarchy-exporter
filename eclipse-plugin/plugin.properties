# Default (English) texts for plugin.xml and MANIFEST.MF.
#
# These are resolved by OSGi (Bundle-Localization in MANIFEST.MF), not by
# jche/eclipse/Messages.java, so they have to live in a separate file at the
# bundle root. Everything the plug-in's own code shows is in
# jche/eclipse/messages.properties instead.
#
# Translations: plugin_<language>.properties (plugin_ja.properties for Japanese).

bundleName = Java Call Hierarchy Exporter
bundleVendor = instreest

category.name = Java Call Hierarchy Exporter
view.name = Impact Analysis (Call Hierarchy Exporter)

command.showCallers.name = Show the caller hierarchy

menu.showCallers.label = Impact Analysis: Show the caller hierarchy

page.name = Impact Analysis (Call Hierarchy Exporter)
