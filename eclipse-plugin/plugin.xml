<?xml version="1.0" encoding="UTF-8"?>
<?eclipse version="3.4"?>
<!--
  文言（name / label）は %キー で書き、plugin.properties（英語）と
  plugin_ja.properties（日本語）から引く。MANIFEST.MF の Bundle-Localization がその置き場所を指す。
  Pleiades は Eclipse 本体の辞書しか持たないので、このプラグインの文言はここで訳すしかない
  （docs/eclipse-plugin-nls-qa.md）。

  画面への追加は1つだけ。呼び出し元階層ビュー「影響調査 (Call Hierarchy Exporter)」である。
  メソッドを選んで「影響調査: 呼び出し元階層を表示」で開き、解析結果はプラグインが常駐で持つ
  （docs/eclipse-plugin-ui-design.md）。CSV はビューの右クリックから書き出す。

  設定ファイル（*.properties）の右クリックからの CSV 一括出力は廃止した。同じことは
  コマンドライン版でできて、こちらは経路（別プロセスをもう1つ起こす作り）を二重に抱えていた
  （docs/eclipse-plugin-ui-simplify-qa.md の Q10）
-->
<plugin>
   <extension point="org.eclipse.ui.commands">
      <category
            id="io.github.instreest.jche.eclipse.category"
            name="%category.name"/>
      <command
            categoryId="io.github.instreest.jche.eclipse.category"
            defaultHandler="jche.eclipse.ShowCallersHandler"
            id="io.github.instreest.jche.eclipse.showCallers"
            name="%command.showCallers.name"/>
   </extension>

   <extension point="org.eclipse.ui.views">
      <category
            id="io.github.instreest.jche.eclipse.viewCategory"
            name="%category.name"/>
      <view
            category="io.github.instreest.jche.eclipse.viewCategory"
            class="jche.eclipse.CallHierarchyView"
            id="io.github.instreest.jche.eclipse.callHierarchyView"
            name="%view.name"
            restorable="true"/>
   </extension>

   <extension point="org.eclipse.ui.menus">
      <!-- メソッドの上での右クリック（エディタ・エクスプローラー・アウトライン）。
           選択の種類は問わない。メソッドが特定できなければハンドラ側が断る -->
      <menuContribution locationURI="popup:org.eclipse.ui.popup.any?after=additions">
         <command
               commandId="io.github.instreest.jche.eclipse.showCallers"
               label="%menu.showCallers.label"
               style="push"/>
      </menuContribution>
   </extension>

   <!-- 設定画面（ウィンドウ > 設定 > 影響調査 (Call Hierarchy Exporter)）。
        解析は別プロセスなので、使う JDK とメモリ（JVM 引数）、そして作ったファイルの
        置き場所をここで決められる。解析キャッシュの削除もここから -->
   <extension point="org.eclipse.ui.preferencePages">
      <page
            class="jche.eclipse.JchePreferencePage"
            id="io.github.instreest.jche.eclipse.preferences"
            name="%page.name"/>
   </extension>

   <!-- Ctrl+Alt+Shift+H。Eclipse 標準の呼び出し階層（Ctrl+Alt+H）とは別物なので割り当てを分ける -->
   <extension point="org.eclipse.ui.bindings">
      <key
            commandId="io.github.instreest.jche.eclipse.showCallers"
            contextId="org.eclipse.ui.contexts.window"
            schemeId="org.eclipse.ui.defaultAcceleratorConfiguration"
            sequence="M1+M2+M3+H"/>
   </extension>
</plugin>
