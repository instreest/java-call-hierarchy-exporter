# --conditions の検査用（bash test/conditions/run.sh）。解析対象は test/demo。
# キャッシュと出力フォルダの場所をこのフォルダの下に向けておき、
# --conditions がどちらも作らないことを検査できるようにする。
project.root=../demo
source.folders=src
source.encoding=UTF-8
cache.folder=./.cache
output.folder=./out
