# ファクトリ＋キー（C-3）の契約表だけで絞る。拡張を使った expected と同じ結果になる
project.root=../../plugin-demo
source.folders=src
library.folders=
source.encoding=UTF-8
source.level=
entry.packages=fxp.App
exclude.packages=java.**,javax.**
cache.enabled=true
cache.folder=./.cache
max.depth=50
max.rows=100000
dataflow.enabled=true
dataflow.max.depth=5
output.encoding=UTF-8
output.folder=./output

# --- 契約表（種類 C: 具象型） ---------------------------------------
# 拡張（resolver.*）は使わず、契約表の 1 行だけで絞れることを見る
contracts.files=contracts-factory.txt
